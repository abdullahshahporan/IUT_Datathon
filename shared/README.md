# Smart Office Shared

Common types and constants for the Smart Office Monitoring System.

## Installation

```bash
cd shared
npm install
```

## Build

```bash
npm run build
```

The package is intentionally small and type-focused. It exists so the backend, frontend, and Discord bot can share the same domain model if you want to wire them to a single package in a later pass.
